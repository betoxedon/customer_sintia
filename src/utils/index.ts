import { setStringToCapital } from '@/utils/setStringToCapital'
import { setStringToId } from '@/utils/setStringToId'
import { setStringToLower } from '@/utils/setStringToLower'
import { setStringToUpper } from '@/utils/setStringToUpper'
import { setNowUtcUnix } from '@/utils/setNowUtcUnix'
import { setUtcUnixToLocaleString } from '@/utils/setUtcUnixToLocaleString'
import { setPlanExpiresUtcUnix } from '@/utils/setPlanExpiresUtcUnix'
import { generateInvalidator } from '@/utils/generateInvalidator'
import { setDataConfirmation, resolveConfirmation } from '@/utils/handleConfirmation'
import { updateStyles } from '@/utils/updateStyles'
import { setColorStyle } from '@/utils/setColorStyle'
import { handleFastApiError } from '@/utils/handleFastApiError'

export {
   setStringToCapital,
   setStringToId,
   setStringToLower,
   setStringToUpper,
   setNowUtcUnix,
   setUtcUnixToLocaleString,
   setPlanExpiresUtcUnix,
   generateInvalidator,
   setDataConfirmation,
   resolveConfirmation,
   updateStyles,
   setColorStyle,
   handleFastApiError,
}
